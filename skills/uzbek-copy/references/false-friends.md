# False friends

RU-mediated traps AIs love.

| English | Bad leak | Prefer by sense |
|---|---|---|
| actual | `aktual` | `haqiqiy` / `dolzarb` |
| magazine | `magazin` | jurnal (publication) vs `doʻkon` (shop) |
| intelligent | `intelligent` | `aqlli` vs `ziyoli` / madaniyatli |
| accurate | `akkurat` vibe | `aniq` / `toʻgʻri` |
| original | `original` spam | `asl` / `original` only if product term |
| perspective | `perspektiva` always | `nuqtai nazar` / `imkoniyat` by sense |

## Rule

If a clear Uzbek word exists, prefer it over an internationalism that only looks smart.
